# Portable dependency specification.
#
# The authoritative source of truth is pyproject.toml; this file exists
# for `pip install -r requirements.txt` users who do not use pip's
# project metadata. Versions were validated in the reference environment:
# Python 3.11.14 / macOS arm64 (Apple MPS) with numpy 1.26, torch 2.5.1.
#
# For a full reproducible environment use environment.yml (conda) or
# `pip install -e ".[dev]"`.

numpy>=1.24,<2.0
pandas>=2.1,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.4,<2.0
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0
optuna>=3.5,<4.0
torch>=2.2,<3.0
mlflow>=3.14,<4.0
shap>=0.44,<1.0
captum>=0.7,<1.0
h5py>=3.10,<4.0
pyyaml>=6.0,<7.0
openpyxl>=3.1,<4.0
xlrd>=2.0,<3.0
joblib>=1.3,<2.0

# dev
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
ruff>=0.3
mypy>=1.8,<2.0
